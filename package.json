{
  "name": "dms",
  "version": "0.0.0",
  "description": "dead-man's switch implemented in electron",
  "main": "./src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemailer": "^4.6.3"
  }
}
